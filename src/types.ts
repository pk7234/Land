export interface MarketData {
  id: string;
  type: string;
  title: string;
  description: string;
  imageUrl: string;
}

export interface Proof {
  id: string;
  imageUrl: string;
  caption: string;
  timestamp: string;
}
